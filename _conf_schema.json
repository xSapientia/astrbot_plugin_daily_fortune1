{
  "jrrp_algorithm": {
    "description": "人品值随机算法",
    "type": "string",
    "default": "hash",
    "options": ["hash", "random"],
    "hint": "hash: 基于用户ID和日期的哈希算法，相同用户相同日期结果固定; random: 纯随机算法"
  },
  "detecting_message": {
    "description": "检测中提示文本",
    "type": "string",
    "default": "神秘的能量汇聚，{nickname}，你的命运即将显现，正在祈祷中...",
    "hint": "支持变量: {nickname}"
  },
  "show_cached_result": {
    "description": "查询时是否显示缓存的完整结果",
    "type": "bool",
    "default": true,
    "hint": "开启后，再次查询时会显示首次生成的完整结果"
  },
  "history_days": {
    "description": "个人历史记录计算与缓存天数",
    "type": "int",
    "default": 30,
    "hint": "保留最近多少天的历史记录"
  },
  "llm_provider_id": {
    "description": "LLM提供商ID",
    "type": "string",
    "default": "",
    "hint": "留空使用AstrBot默认提供商，填写则使用指定的provider_id"
  },
  "llm_api": {
    "description": "第三方LLM接口配置",
    "type": "object",
    "items": {
      "api_key": {
        "description": "API密钥",
        "type": "string",
        "default": ""
      },
      "url": {
        "description": "API地址",
        "type": "string",
        "default": "",
        "hint": "支持填写到/v1或/v1/chat/completions"
      },
      "model": {
        "description": "模型名称",
        "type": "string",
        "default": "gpt-3.5-turbo"
      }
    }
  },
  "persona_name": {
    "description": "使用的人格名称",
    "type": "string",
    "default": "",
    "hint": "留空使用AstrBot默认人格，填写则使用指定的人格"
  },
  "prompts": {
    "description": "LLM提示词配置",
    "type": "object",
    "items": {
      "process": {
        "description": "过程模拟提示词",
        "type": "text",
        "default": "使用user_id的简称称呼，模拟你使用水晶球缓慢复现今日结果的过程，50字以内",
        "hint": "支持变量: {nickname}, {card}, {title}, {jrrp}, {fortune}, {femoji}"
      },
      "advice": {
        "description": "评语建议提示词",
        "type": "text",
        "default": "使用user_id的简称称呼，对user_id的今日人品值{jrrp}给出你的评语和建议，50字以内",
        "hint": "支持变量: {nickname}, {card}, {title}, {jrrp}, {fortune}, {femoji}"
      }
    }
  },
  "templates": {
    "description": "显示模板配置",
    "type": "object",
    "items": {
      "random": {
        "description": "首次查询结果模板",
        "type": "text",
        "default": "🔮 {process}\n💎 人品值：{jrrp}\n✨ 运势：{fortune}\n💬 建议：{advice}。",
        "hint": "支持变量: {process}, {jrrp}, {fortune}, {advice}"
      },
      "query": {
        "description": "再次查询结果模板",
        "type": "text",
        "default": "📌 今日人品\n{nickname}，今天已经查询过了哦~\n今日人品值: {jrrp}\n运势: {fortune} {femoji}",
        "hint": "支持变量: {nickname}, {jrrp}, {fortune}, {femoji}"
      },
      "rank": {
        "description": "排行榜条目模板",
        "type": "string",
        "default": "{medal} {nickname}: {jrrp} ({fortune})",
        "hint": "支持变量: {medal}, {nickname}, {jrrp}, {fortune}"
      },
      "board": {
        "description": "排行榜整体模板",
        "type": "text",
        "default": "📊【今日人品排行榜】{date}\n━━━━━━━━━━━━━━━\n{ranks}",
        "hint": "支持变量: {date}, {ranks}"
      },
      "history": {
        "description": "历史记录模板",
        "type": "text",
        "default": "📚 {nickname} 的人品历史记录\n{history}\n\n📊 统计信息:\n平均人品值: {avgjrrp}\n最高人品值: {maxjrrp}\n最低人品值: {minjrrp}",
        "hint": "支持变量: {nickname}, {history}, {avgjrrp}, {maxjrrp}, {minjrrp}"
      }
    }
  },
  "delete_data_on_uninstall": {
    "description": "卸载时是否删除缓存数据",
    "type": "bool",
    "default": false,
    "hint": "开启后卸载插件时会删除所有用户数据"
  },
  "delete_config_on_uninstall": {
    "description": "卸载时是否删除配置文件",
    "type": "bool",
    "default": false,
    "hint": "开启后卸载插件时会删除配置文件"
  }
}
