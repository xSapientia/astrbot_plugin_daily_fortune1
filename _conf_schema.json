{
  "enable_plugin": {
    "description": "是否启用插件",
    "type": "bool",
    "default": true
  },
  "jrrp_algorithm": {
    "description": "人品值随机算法",
    "type": "string",
    "options": ["hash", "random", "seed"],
    "default": "hash",
    "hint": "hash: 基于用户ID和日期的哈希值; random: 真随机; seed: 基于种子的伪随机"
  },
  "cache_days": {
    "description": "结果缓存天数",
    "type": "int",
    "default": 1,
    "hint": "缓存结果的保存天数"
  },
  "history_days": {
    "description": "历史记录天数",
    "type": "int",
    "default": 30,
    "hint": "显示最近多少天的历史记录"
  },
  "show_cached_result": {
    "description": "查询时显示缓存的完整结果",
    "type": "bool",
    "default": false
  },
  "detecting_message": {
    "description": "检测中提示文本",
    "type": "string",
    "default": "神秘的能量汇聚，{nickname}，你的命运即将显现，正在祈祷中...",
    "hint": "支持变量: {nickname}, {card}, {title}"
  },
  "process_prompt": {
    "description": "过程模拟提示词",
    "type": "text",
    "default": "使用user_id的简称称呼，模拟你使用水晶球缓慢复现今日结果的过程，50字以内",
    "hint": "支持变量: {nickname}, {card}, {title}, {jrrp}, {fortune}"
  },
  "advice_prompt": {
    "description": "评语建议提示词",
    "type": "text",
    "default": "使用user_id的简称称呼，对user_id的今日人品值{jrrp}给出你的评语和建议，50字以内",
    "hint": "支持变量: {nickname}, {card}, {title}, {jrrp}, {fortune}"
  },
  "result_template": {
    "description": "随机结果模板",
    "type": "text",
    "default": "🔮 {process}\n💎 人品值：{jrrp}\n✨ 运势：{fortune}\n💬 建议：{advice}",
    "hint": "支持所有变量"
  },
  "query_template": {
    "description": "查询结果模板",
    "type": "text",
    "default": "📌 今日人品\n{nickname}，今天已经查询过了哦~\n今日人品值: {jrrp}\n运势: {fortune} {femoji}",
    "hint": "支持所有变量"
  },
  "rank_template": {
    "description": "排行榜模板",
    "type": "text",
    "default": "📊【今日人品排行榜】{date}\n━━━━━━━━━━━━━━━\n{ranks}",
    "hint": "支持变量: {date}, {ranks}"
  },
  "rank_item_template": {
    "description": "排行榜项目模板",
    "type": "string",
    "default": "{medal} {nickname}: {jrrp} ({fortune})",
    "hint": "支持变量: {medal}, {nickname}, {jrrp}, {fortune}"
  },
  "history_template": {
    "description": "历史记录模板",
    "type": "text",
    "default": "📚 {nickname} 的人品历史记录\n{history}\n\n📊 统计信息:\n平均人品值: {avgjrrp}\n最高人品值: {maxjrrp}\n最低人品值: {minjrrp}",
    "hint": "支持所有统计变量"
  },
  "llm": {
    "description": "LLM配置",
    "type": "object",
    "items": {
      "provider_id": {
        "description": "指定使用的Provider ID",
        "type": "string",
        "hint": "留空使用默认Provider。如果填写了此项，下面的api_key、api_url、model可以不填"
      },
      "persona_name": {
        "description": "指定使用的人格名称",
        "type": "string",
        "hint": "留空使用默认人格"
      },
      "api_key": {
        "description": "自定义OpenAI兼容API密钥",
        "type": "string",
        "hint": "当provider_id为空时生效。支持OpenAI、DeepSeek等兼容接口"
      },
      "api_url": {
        "description": "自定义API地址",
        "type": "string",
        "hint": "智能识别URL格式，支持填写到/v1或/v1/chat/completions",
        "default": ""
      },
      "model": {
        "description": "使用的模型名称",
        "type": "string",
        "hint": "如gpt-3.5-turbo、deepseek-chat等",
        "default": "gpt-3.5-turbo"
      }
    }
  }
}
